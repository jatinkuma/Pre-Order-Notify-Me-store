{%- style -%}
  .saas-cta {
    padding: var(--space-12) 0 !important;
    background: var(--surface-0) !important;
  }
  .saas-cta-container {
    background: var(--surface-1) !important;
    border: 1px solid var(--surface-3) !important;
    border-radius: 40px !important;
    padding: 100px 40px !important;
    margin: 0 40px !important;
    text-align: center !important;
    position: relative;
    overflow: hidden;
    box-shadow: var(--glow-brand) !important;
  }
  .saas-cta-container::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle at center, rgba(198, 255, 0, 0.05), transparent 60%);
    pointer-events: none;
  }
  .saas-cta-heading {
    color: var(--text-primary) !important;
    font-size: 56px !important;
    font-weight: 800 !important;
    letter-spacing: -0.04em !important;
    margin-bottom: 24px !important;
    line-height: 1.1 !important;
    position: relative;
    z-index: 2;
  }
  .saas-cta-text {
    color: var(--text-secondary) !important;
    font-size: 20px !important;
    max-width: 600px !important;
    margin: 0 auto 48px !important;
    line-height: 1.6 !important;
    position: relative;
    z-index: 2;
  }
  .button--cta {
    background: var(--color-brand-1) !important;
    color: #000000 !important;
    border-radius: var(--radius-pill) !important;
    padding: 20px 48px !important;
    font-weight: 700 !important;
    font-size: 18px !important;
    text-decoration: none !important;
    display: inline-flex !important;
    align-items: center !important;
    gap: 12px !important;
    transition: var(--transition-spring) !important;
    position: relative;
    z-index: 2;
  }
  .button--cta:hover {
    transform: translateY(-4px) scale(1.02);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
  }
  @media screen and (max-width: 749px) {
    .saas-cta-container {
      margin: 0 20px !important;
      padding: 60px 24px !important;
    }
    .saas-cta-heading {
      font-size: 36px !important;
    }
  }
{%- endstyle -%}

<section class="saas-cta">
  <div class="page-width">
    <div class="saas-cta-container">
      <h2 class="saas-cta-heading">{{ section.settings.title }}</h2>
      <p class="saas-cta-text">{{ section.settings.text }}</p>
      <a href="{{ section.settings.button_link | default: 'https://apps.shopify.com/ests-preorder-notify' }}" class="button--cta">
        {{ section.settings.button_label }} <span>→</span>
      </a>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "SaaS CTA Section",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "inline_richtext",
      "id": "title",
      "default": "Ready to Transform Your Store?",
      "label": "Heading"
    },
    {
      "type": "textarea",
      "id": "text",
      "default": "Join 2,000+ Shopify Plus merchants scaling their business with our premium solution.",
      "label": "Description"
    },
    {
      "type": "text",
      "id": "button_label",
      "default": "Start Free Trial",
      "label": "Button Label"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    }
  ],
  "presets": [
    {
      "name": "SaaS CTA Section"
    }
  ]
}
{% endschema %}
